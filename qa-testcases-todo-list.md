# todo-list.html QAテストケース一覧

対象: `Schedule/todo-list.html`（静的コードレビューに基づく再現手順の一覧。ブラウザでの実操作による最終確認は未実施）

各ケースは「手順 → 期待動作 → コードから読み取れる実際の動作」の順。

## 対応状況（2026-09-10 更新・コードレビューベース）

以下の項目はコード側で対応済みと確認した（実ブラウザでの再確認は未実施）。

| ID | 状況 | 対応内容 |
|---|---|---|
| A-1 | ✅対応済み | インポート時に`filter(t => t && typeof t === "object" && !Array.isArray(t))`で不正な要素を除外 |
| A-2 | ✅対応済み | 起動時（`loadData()`直後）・インポート時の両方に`if (!state.settings) state.settings = {}`等のガードを追加 |
| A-3 | ✅対応済み | `normalizeEstimate()`を追加し、`migrateLegacyState()`内でestimateを0.25〜8にクランプ |
| B-1 | ✅対応済み | 追加フォーム・行内編集の両方で`deadline < start`なら`deadline = start`に自動補正 |
| B-2 | ✅対応済み | `window.open(googleCalendarUrl(task), ...)`をtry/catchで囲み、失敗時に分かりやすいアラートを表示 |
| B-3 | ✅対応済み（軽微） | 挙動は変更せず、設定ヒントに「時刻ありで開始日未入力の場合は登録日が使われる」旨を追記 |
| C-3 | ✅対応済み | `dedupeNames()`を追加し、`migrateLegacyState()`でカテゴリ名の重複を解消 |
| D-2 | ✅対応済み | 上記`dedupeNames()`が締め区分（cycleGroups）にも適用され、重複状態自体が読み込み時に解消される |
| E-1 | ✅対応済み | インポート時に全タスクへ`id: uid()`を振り直す |
| F-1 | ✅対応済み | 6〜19の壊れた値は最低優先度(1)ではなく最大側(5)に倒す設計に変更 |
| F-2 | ✅対応済み（今回修正） | `normalizeImportance5()`冒頭で`value === null \|\| value === undefined`を先に判定し、nullとundefinedを同じ既定値(3)にした |
| G-1 | ✅対応済み | 負荷キャパが0以下の時は「◯% / 上限未設定」とグレー表示にし、誤って正常色（緑）にならないようにした |
| C-1, C-2, D-1, D-3, H-1 | ー | 元々問題なしと確認済み（対応不要） |

いずれもコードレビューでの確認であり、実ブラウザでの動作確認は行っていない点に注意。

## A. インポート機能・データ破損系

### A-1. tasks配列の要素がオブジェクトでない
- 手順: エクスポート済みJSONを編集し、`"tasks": [null, "abc", 123]` のような要素を混ぜて📂インポート
- 期待動作: 不正な要素を無視するか、エラーメッセージを出して安全に中断する
- 実際: `migrateLegacyState()`内の`t.importance = normalizeImportance5(t.importance)`が`t`がnull等の場合に`TypeError`を送出。インポート処理は`imported.tasks`が配列であることしかチェックしておらず、要素の型は検証していない（`todo-list.html` 1700行目付近の検証条件、896-929行目の`migrateLegacyState`参照）。例外発生によりインポートの`saveData()`以降（保存・再描画・完了アラート）が実行されず、以後のタスク一覧再描画（`renderTasks`のタイトル参照など）でも同様の例外が再発し、リロードするまで画面操作が壊れる。

### A-2. localStorage直下のJSONに`settings`キーが無い
- 手順: DevTools等で`localStorage["workCapacityApp_v1"]`を`{"tasks":[]}`のように`settings`なしで保存し、ページを再読み込み
- 期待動作: 既定値で補完して正常起動する（📂インポート時と同様の挙動）
- 実際: 起動直後の`if (state.settings.reminderMinutes === undefined) ...`（852行目）が`state.settings`未定義のまま参照するため`TypeError`。📂インポート側は`if (!state.settings) state.settings = {}`（1709行目）で保護されているが、通常のページロード経路（`loadData()`→852行目）には同じ保護がない。アプリ全体が真っ白になる可能性がある。

### A-3. 見積もり時間（estimate）が未正規化のまま保持される
- 手順: `estimate`が`-5`や`"abc"`、`999999`などのタスクを含むJSONをインポート
- 期待動作: load/importanceと同様に妥当な範囲へ正規化される
- 実際: `migrateLegacyState()`は`load`と`importance`のみ正規化しており、`estimate`は無変換のまま保持される（896-929行目）。結果、一覧の見積もりセレクト（`estimateOptions`）は一致する`<option>`が無いため見た目上は先頭候補（0.25h）が選択されているように見えるが、Todoリストの表示は`Number(t.estimate) || 0`により「見積0h」と表示され、同一タスクなのに一覧とTodoで見積表示が食い違う（1474行目, 2026行目）。またGoogleカレンダー登録時は`(Number(task.estimate) || 1) * 3600000`がそのまま使われ、負値なら終了時刻が開始時刻より前になる（1211行目）。

## B. 開始日・終了日・時刻の不整合

### B-1. 終了日が開始日より前
- 手順: タスク一覧の行内編集で、開始日を2026-09-10、終了日を2026-09-01に設定
- 期待動作: 警告を出すか、入力を拒否する
- 実際: フォーム・行内編集ともに開始日/終了日の前後関係チェックは無い（`taskBody`のchangeハンドラ、1852-1871行目）。ガントチャート側は`buildGanttItems()`内で`if (ed < sd) ed = sd;`により表示上は「終了日=開始日」の1日バーとして描画される（2143行目）。一方`taskDetails()`や`googleCalendarUrl`はtask.deadlineの生値をそのまま使うため、ガントチャートの表示（1日分）とカレンダー登録・詳細表示（元の矛盾した終了日）が食い違う。データ自体は矛盾したまま保存され続ける。

### B-2. 時刻ありだが開始日が壊れた文字列（不正な日付文字列）
- 手順: localStorage/インポートJSONで`start`を`"2026-13-40"`のような不正な日付文字列にし、時刻ありのタスクを「📅追加」または「☁️追加」
- 期待動作: エラーメッセージを表示するか、その項目をスキップする
- 実際: `taskStartEnd()`が`new Date("2026-13-40T00:00:00")`でInvalid Dateを生成し、`fmtGCal(d) => d.toISOString()`（1236行目）が`RangeError: Invalid time value`を送出する。未連携時（`window.open(googleCalendarUrl(task), "_blank")`、1647行目）はtry/catch外で呼ばれるため、クリックしても何も起きず、ユーザーには理由が分からないまま失敗する。連携済み時は`addEventToGcalDirect`がasync関数内の例外としてPromise rejectionになり、`.catch(err => alert(err.message))`（1645行目）で"Invalid time value"という技術的なアラートが出る。

### B-3. 時刻のみ設定で開始日が空
- 手順: 開始日を空にし、時刻だけ入力してカレンダー追加
- 期待動作: 明示されている「終日予定」ロジックとの整合
- 実際: `taskCalendarRange()`は`task.time`があれば無条件に時刻ありパスへ行き、`taskStartEnd`のfallbackで実行時の「今日」の日付が補われる（1207-1226行目）。ヒント文言「時刻が未入力のタスクは終日予定」は開始日未入力×時刻ありの組み合わせを想定していないため、ユーザーが意図しない日付（実行した日）でカレンダー登録される可能性がある。

## C. カテゴリの仕事/プライベート区分と負荷集計

### C-1. カテゴリ削除後の負荷集計
- 手順: 「仕事」カテゴリのタスクを作成→負荷集計に反映されることを確認→そのカテゴリを削除
- 期待動作（ヒント文言通り）: 削除後はそのタスクが「カテゴリなし」になり、仕事の負荷集計から外れる
- 実際: `deleteCategory()`が該当タスクの`t.category`を`""`にクリアするため（2322行目）、`isWorkCategory("")`は先頭の`if (!categoryName) return false;`で弾かれ、負荷集計から確実に除外される。ドキュメント通りで問題なし（バグではないが動作確認済みとして記録）。

### C-2. カテゴリの仕事/プライベート区分を事後変更
- 手順: 「仕事」カテゴリのタスクがある状態で、カテゴリ管理タブでそのカテゴリを「プライベート」に切り替え
- 期待動作: 負荷集計が即座に付け替わる
- 実際: `isWorkCategory`/`isPrivateCategory`は毎回`state.categories`を名前で検索するライブ判定のため、`setCategoryType()`実行後の`renderAll()`で即時に仕事負荷→プライベート負荷へ移動する。問題なし。

### C-3. カテゴリ名重複（インポート等で発生した場合）
- 手順: 同名カテゴリ（例："研究"）が2件、type違い（work/private）で存在するJSONをインポート
- 期待動作: 名前の一意性が保証されるか、区別できる
- 実際: 通常UIの「追加」「改名」は重複チェックがあるため作れないが、インポートJSONには重複チェックが無い。`isWorkCategory`/`isPrivateCategory`は`state.categories.find(...)`で**最初に一致した1件のみ**を見るため、どちらの"研究"を使っているタスクも常に最初のエントリのtypeで判定される。削除・改名も同様に最初の1件（`state.categories.find`/`indexOf`相当）にしか作用しないため、UIで2番目の行を操作しても実際には1番目のデータが変化する不整合が起きうる。

## D. タスク一覧下のタブ（cycleGroups）

### D-1. タブが1つの状態での削除
- 手順: ⚙️設定でタブを1つだけ残し、削除を試みる
- 期待動作: 削除できない
- 実際: `deleteCycleGroup()`冒頭の`if (state.cycleGroups.length <= 1) { alert(...); return; }`（1083行目）と、削除ボタン自体が`disabled`（1054, 1060-1061行目）で二重に防止されている。問題なし。

### D-2. タブ名の重複（インポート等で発生した場合）
- 手順: `cycleGroups: ["日常業務", "日常業務", "プライベート"]`のようなJSONをインポートし、重複した「日常業務」タブの片方を削除
- 期待動作: クリックした1件のみ削除される
- 実際: `deleteCycleGroup(name)`は`state.cycleGroups.filter(g => g !== name)`（1085行目）で**名前が一致する全件**を除去するため、重複していた2つの「日常業務」タブが同時に消え、それらのタスクは残った「プライベート」に一括で移動する。確認ダイアログの文言（「〜を削除しますか？」）は1件の削除を想定した表現になっており、実際の挙動と食い違う。カテゴリ削除（`deleteCategory`）にも同じ実装パターン（`filter(c => c.name !== name)`）があり、同種の問題を抱える。
- 備考: 通常のUI操作（追加・改名時の重複チェックあり）だけでは再現できず、インポートまたはlocalStorage直接編集が必要。

### D-3. タブ名変更時の既存タスクへの追従
- 手順: 「日常業務」タブを「業務A」に改名
- 期待動作: そのタブに属するタスクのcycleGroupも追従する
- 実際: `renameCycleGroup()`が`state.tasks.forEach(t => { if (t.cycleGroup === oldName) t.cycleGroup = newName; })`（1074行目）で追従させており、問題なし。

## E. タスクIDの欠落（旧式・手編集データ）

### E-1. `id`フィールドを持たないタスクをインポート
- 手順: `id`キーを削除した状態のタスクを含むJSONをインポート
- 期待動作: 自動的にIDが割り当てられ、編集・削除・チェックが機能する
- 実際: `migrateLegacyState()`はid未設定の補完を行っていない（896-929行目）。描画時は`data-id="${t.id}"`で`undefined`が文字列"undefined"としてDOMに書き込まれるが、クリック/change時の検索は`state.tasks.find(t => t.id === id)`で`t.id`（実際の値`undefined`）と`id`（DOMから取得した文字列"undefined"）を比較するため一致せず、`task`が見つからず操作が黙って無視される（削除・複製・チェック・インライン編集いずれも効かない）。

## F. 優先度(importance)の境界値・レガシー変換

### F-1. importance = 6〜19 の範囲
- 手順: `importance: 8`のようなタスクをインポート
- 期待動作: 明確な仕様は無いが、直感的には高優先度寄りに解釈されるのが自然
- 実際: `normalizeImportance5()`は`n > 5`を「旧・0〜100%形式」とみなし`Math.round(n/20)`で変換するため、6〜19の値はすべて`round(n/20)=0→||1`で**最低優先度(1)**に変換される（882行目）。感覚的には「8」を高優先度と誤解しやすく、実装意図（0-100%スケールの名残）を知らないと驚く結果になる。

### F-2. importance = null と undefined の扱いの違い
- 手順: `importance: null`のタスクと、importanceキー自体が無いタスクをそれぞれインポート
- 期待動作: 同じ「未設定」として同じデフォルト値になる
- 実際: `Number(null) === 0`のため`normalizeImportance5(null)`は`n===0`分岐に入り**1**になる。一方`Number(undefined)`は`NaN`のため`isNaN(n)`分岐で**3**になる（878-884行目）。同じ「値が無い」状況でも、`null`と`undefined`とで最終的な優先度が異なる。

## G. 負荷キャパ(workLoadCap/privateLoadCap) = 0 または負値

### G-1. 負荷キャパを0に設定
- 手順: ⚙️設定で「仕事の負荷キャパ」を0にして保存し、負荷50%のタスクがある状態で表示
- 期待動作: キャパ超過（危険）として警告色になる
- 実際: `renderWorkLoad()`で`cap > 0 ? Math.round(...) : 0`（1191行目）のため、キャパ0の時は常に`pct=0`となり`pctColor(0)`は緑（正常）になる。実際には負荷50%に対してキャパ0という明らかな超過状態なのに、表示上は正常色になり矛盾する。
- 備考: `<input type="number" min="0">`はブラウザの見た目の制約であり、直接値を入力すれば負の値も設定可能（`saveSettingsBtn`のクリックハンドラにはmin側のバリデーションが無い）。負のキャパでも同様に`cap > 0`が偽になり同じ表示になる。

## H. 長いタイトル・特殊文字

### H-1. 極端に長いタイトル・絵文字
- 手順: 200文字超のタイトルや絵文字混じりのタイトルでタスクを追加
- 期待動作: レイアウト崩れやXSSが起きない
- 実際: `escapeHtml()`が`& < > " '`を適切にエスケープしており、`value="..."`属性内でも安全（1500-1502行目）。CSS側も`.title-cell input.inline-edit`や`.gantt2-row-label`、`.gb-label`に`overflow:hidden; text-overflow:ellipsis; white-space:nowrap;`が設定されており、レイアウト崩れは起きにくい（335-337, 464-467, 498行目）。この観点では問題は見つからなかった。

---

## 未確認・要注意（実ブラウザでの確認が必要）

- 上記のうち、実際にブラウザで操作して例外・画面崩れを目視確認したものは無い（ツール制約によりコード読解のみ）。特にA-1・A-2・B-2のようなJavaScript例外の発生箇所は、実ブラウザのコンソールで再現確認することを推奨する。
- Google Calendar/Tasks API連携部分（OAuth・実際のAPIレスポンス）は、実アカウントでの接続確認が必要で未検証。
- ドラッグ＆ドロップ（Pointer Events自前実装）のタッチデバイス・トラックパッドでの実際の挙動は未検証。
